<%= render 'page_title' %>

<div class="row">
  <div class="col-md-8">
    <%= render "current_enrollments" %>
    <% if @user.sections.any? %>
      <%= render "instructor_sections" %>
    <% end %>
  </div>
  <div class="col-md-4">
    <%= render "report_card" %>
  </div>
</div>
