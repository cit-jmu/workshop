Hi, <%= @user.display_first %>!

You have been enrolled in the <%= Rails.application.config.x["terminology"]["workshop"] %>: <%= @course.title %>

<%= @part.date_and_time %> (<%= @part.location %>)

* If this is a multi-<%= Rails.application.config.x["terminology"]["part"].downcase %> <%= Rails.application.config.x["terminology"]["course"] %>, then you will receive individual emails
for each <%= Rails.application.config.x["terminology"]["part"].downcase %>.

To unenroll from this <%= Rails.application.config.x["terminology"]["course"] %>, please copy and paste this link into your
browser:

<%= confirm_unenroll_course_section_url(@course, @section) %>

======================================================================
<%= @course.title %>
Duration: <%= @course.duration %>
<% @section.parts.each do |part| %>
  - <%= part.date_and_time %> (<%= part.location %>)
<% end %>
----------------------------------------------------------------------
<%= @course.description %>
